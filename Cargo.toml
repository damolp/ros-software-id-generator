[package]
name = "rosgen"
version = "0.2.0"
edition = "2021"
description = "RouterOS System Identifier Generator"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"

[profile.release]
opt-level = 3
lto = true
